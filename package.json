{
  "name": "vista-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w frontend",
    "start": "serve -s frontend/dist -l ${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
